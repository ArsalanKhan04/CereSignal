# Backend URL — set to your Railway backend URL in production
REACT_APP_API_BASE_URL=http://localhost:8000/api/v1

# Optional: override log level (debug | info | warn | error)
# REACT_APP_LOG_LEVEL=info
